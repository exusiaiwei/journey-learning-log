#show: project.with(
  title: "Test",
  authors: (
    (name: "Zichao Wei", email: "exusiai.wei@outlook.com"),
  ),
  // Insert your abstract after the colon, wrapped in brackets.
  // Example: `abstract: [This is my abstract...]`
  abstract: [摘要],
  date: "December 5, 2023",
)

// 第一段话
这是一个使用typst格式的示例代码段。它包含了一个导入语句和一些简单的数学运算。

// 第二段话
在这个代码段中，我们定义了一些整数变量，并进行了一些比较运算。例如，我们使用了等于运算符（==）来比较两个数是否相等。

// 第三段话
除了数学运算，我们还可以在typst中使用其他功能。例如，我们可以导入其他文件并使用其中定义的函数和变量。

// 随机公式
int result = (a + b) 
 123123123

 = 1
 = 22
 == 22
=== 333

123123123123123123